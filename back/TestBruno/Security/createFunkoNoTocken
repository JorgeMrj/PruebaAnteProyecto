meta {
  name: Funkos - User Cannot Create
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/funkos
  body: multipartForm
  auth: bearer
}

docs {
  # Funkos - User Cannot Create
  
  Verifica que un usuario estándar (rol USER) no pueda crear un Funko.
  
  ## Respuesta Esperada
  - **403 Forbidden**: El usuario no tiene permisos suffisientes.
}

auth:bearer {
  token: {{userToken}}
}

body:multipart-form {
  nombre: Test Funko
  price: 19.99
  categoria: Test
}

tests {
  test("Status should be 403", function() {
    expect(res.status).to.equal(403);
  });
}